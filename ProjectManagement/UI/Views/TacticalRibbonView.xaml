<UserControl x:Class="WpfResourceGantt.ProjectManagement.UI.Views.TacticalRibbonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:ui="clr-namespace:WpfResourceGantt.ProjectManagement.UI"
             d:DesignHeight="100" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Icons.xaml"/>
                <ResourceDictionary Source="../Styles/CoreStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    
    </UserControl.Resources>
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Setter Property="Height" Value="100"/>
            <Style.Triggers>
                <!-- Collapse Height when icons are hidden -->
                <DataTrigger Binding="{Binding IsRibbonExpanded}" Value="False">
                    <Setter Property="Height" Value="45"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    <Grid Background="{StaticResource TacticalRibbonBackgroundBrush}">
        <!-- Only one row needed now for navigation -->
        <Border BorderBrush="{StaticResource TacticalSeparatorBrush}" BorderThickness="0,0,0,1" Padding="10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Navigation -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Separator -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Management -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- NEW: Separator -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- NEW: Actions -->
                    <ColumnDefinition Width="*"/>
                    <!-- Spacer -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Toggle -->
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition x:Name="LabelRow">
                        <RowDefinition.Style>
                            <Style TargetType="RowDefinition">
                                <Setter Property="Height" Value="20"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRibbonExpanded}" Value="False">
                                        <Setter Property="Height" Value="0"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </RowDefinition.Style>
                    </RowDefinition>
                </Grid.RowDefinitions>

                <!-- SECTION 1: VIEWS -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="DASHBOARD" Tag="{StaticResource Icon_Dashboard}" 
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_Dashboard_Active}"
                            CommandParameter="Dashboard"
                            Command="{Binding DataContext.ShowDashboardCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="PROJECTS" Tag="{StaticResource Icon_Projects}" 
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_Projects_Active}"
                            CommandParameter="Projects"
                            Command="{Binding DataContext.ShowProjectManagementCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="RESOURCE&#x0a;GANTT" Tag="{StaticResource Icon_ResourceGantt}" 
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_ResourceGantt_Active}"
                            CommandParameter="Resource Gantt"
                            Command="{Binding DataContext.ShowGanttCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="ANALYTICS" Tag="{StaticResource Icon_Analytics}" 
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_Analytics_Active}"
                            CommandParameter="Analytics"
                            Command="{Binding DataContext.ShowAnalyticsCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>

                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="EVM" Tag="{StaticResource Icon_EVM}"
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_EVM_Active}"
                            CommandParameter="EVM"
                            Command="{Binding DataContext.ShowEVMCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="PRIMARY NAVIGATION" Style="{StaticResource TacticalCategoryLabelStyle}"/>

                <Rectangle Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Style="{StaticResource TacticalSeparatorStyle}" Margin="15,10"/>

                <!-- SECTION 2: MANAGEMENT -->
                <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="USERS" Tag="{StaticResource Icon_Users}"
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_Users_Active}"
                            CommandParameter="Users"
                            Command="{Binding DataContext.ShowUserManagementCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" Content="SYSTEMS" Tag="{StaticResource Icon_Systems}" 
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_Systems_Active}"
                            CommandParameter="Systems"
                            Command="{Binding DataContext.ShowSystemManagementCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                </StackPanel>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="ADMINISTRATION" Style="{StaticResource TacticalCategoryLabelStyle}"/>

                <Rectangle Grid.Row="0" Grid.RowSpan="2" Grid.Column="3" Style="{StaticResource TacticalSeparatorStyle}" Margin="15,10"/>

                <!-- SECTION 3: ACTIONS -->
                <StackPanel Grid.Row="0" Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" 
                            Content="ASSIGN&#x0a;TASK" 
                            Tag="{StaticResource Icon_AssignTask}" 
                            ui:RibbonProperties.ActiveIcon="{StaticResource Icon_AssignTask_Active}"
                            CommandParameter="QuickTasks"
                            Command="{Binding DataContext.OpenAddTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            ToolTip="Create a new Quick Task"/>

                    <!-- CLOSE WEEK: Captures frozen weekly EVM snapshot for all SubProjects.  -->
                    <!-- PM / FlightChief only. Disabled for Developers via CanExecute.        -->
                    <Button Style="{StaticResource TacticalRibbonButtonStyle}" 
                            Content="CLOSE&#x0a;WEEK" 
                            Tag="{StaticResource Icon_Import}"
                            Command="{Binding DataContext.CloseWeekCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            ToolTip="Freeze weekly EVM snapshot for all SubProjects (PM only). Complete SMTS import before closing the week."/>
                </StackPanel>
                <TextBlock Grid.Row="1" Grid.Column="4" Text="ACTIONS" Style="{StaticResource TacticalCategoryLabelStyle}"/>
                <!-- NEW: RIBBON TOGGLE BUTTON -->
                <Button Grid.Column="6" Grid.RowSpan="2" 
                        VerticalAlignment="Bottom" Margin="0,0,0,5"
                        Style="{StaticResource {x:Type Button}}"
                        Background="Transparent"
                        Command="{Binding ToggleRibbonCommand}"
                        ToolTip="Toggle Ribbon Icons">
                    <Path x:Name="ToggleArrow" 
                          Data="{StaticResource Icon_ChevronDown}" 
                          Fill="{StaticResource TacticalGoldBrush}" 
                          Width="12" Height="12" Stretch="Uniform" RenderTransformOrigin="0.5,0.5">
                        <Path.RenderTransform>
                            <RotateTransform Angle="180"/>
                        </Path.RenderTransform>
                    </Path>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>

