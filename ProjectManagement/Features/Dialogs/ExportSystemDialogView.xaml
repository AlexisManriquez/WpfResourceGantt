<UserControl x:Class="WpfResourceGantt.ProjectManagement.Features.Dialogs.ExportSystemDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="400" Height="180">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../UI/Styles/CoreStyles.xaml"/>
                <ResourceDictionary Source="../../UI/Styles/Icons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="{StaticResource TacticalBackgroundBrush}" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Select System to Export" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

            <ComboBox Grid.Row="1" 
                      ItemsSource="{Binding AvailableSystems}" 
                      SelectedItem="{Binding SelectedSystem}" 
                      DisplayMemberPath="Name" 
                      VerticalAlignment="Center" 
                      Padding="5"
                      FontSize="14"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="CANCEL" Command="{Binding CancelCommand}" Width="80" Padding="5" Margin="0,0,10,0"/>
                <Button Content="EXPORT" Command="{Binding ConfirmCommand}" Width="80" Padding="5" Style="{StaticResource TacticalPrimaryButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>