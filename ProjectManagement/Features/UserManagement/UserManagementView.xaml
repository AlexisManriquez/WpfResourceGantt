<UserControl x:Class="WpfResourceGantt.ProjectManagement.Features.UserManagement.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{StaticResource AppBackgroundBrush}">
    <UserControl.Resources>
        <!-- Simple style for DataGrid headers -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TacticalTextMutedBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,10,10,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}"/>
        </Style>

        <!-- Style for rows -->
        <Style TargetType="DataGridRow">
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource TacticalSurfaceLightBrush}"/>
                </Trigger>
                <!-- Selection Highlight -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource TacticalGoldBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource TacticalTextInverseBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for Cell -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource TacticalTextInverseBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Grid.Column="0">
                <TextBlock Text="User Management" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Manage system users, sections, and their roles" FontSize="14" Foreground="{StaticResource TacticalTextMutedBrush}" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- REMOVED: Add User Button from here -->
        </Grid>
        <!-- Users Table -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding SelectedUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  IsReadOnly="True"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  Background="{StaticResource SurfaceBrush}"
                  BorderThickness="1"
                  BorderBrush="{StaticResource BorderBrush}">

            <DataGrid.Columns>
                <!-- Name -->
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Section (Acting as Email col in screenshot) -->
                <DataGridTextColumn Header="Section" Binding="{Binding Section}" Width="2*" Foreground="{StaticResource TacticalTextMutedBrush}"/>

                <!-- Role (Styled as Badge) -->
                <DataGridTemplateColumn Header="Role" Width="1.5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource AppBackgroundBrush}" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Role}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TacticalGoldBrush}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>