<UserControl x:Class="WpfResourceGantt.ProjectManagement.Features.Gantt.GateProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfResourceGantt.ProjectManagement.Features.Gantt"
             xmlns:conv="clr-namespace:WpfResourceGantt.ProjectManagement.Converters"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource AppBackgroundBrush}">
    <UserControl.Resources>
        <!-- Reusing Toggle Button Style -->
        <Style x:Key="ExpanderStyle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent" Width="24" Height="24">
                            <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 8 0" Stroke="#9CA3AF" StrokeThickness="2" 
                                   RenderTransformOrigin="0.5,0.5" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="0"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Arrow" Property="Stroke" Value="#EAEAEA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 1. GRIP ICON BRUSH -->
        <VisualBrush x:Key="GripBrush" TileMode="Tile" Viewport="0,0,4,4" ViewportUnits="Absolute">
            <VisualBrush.Visual>
                <Canvas Width="4" Height="4">
                    <Ellipse Fill="#3C4148" Width="2" Height="2" Canvas.Left="1" Canvas.Top="1"/>
                </Canvas>
            </VisualBrush.Visual>
        </VisualBrush>

        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:LevelToIndentConverter x:Key="LevelToIndentConverter"/>
        <conv:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
        <conv:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>

        <!-- NEW: Hierarchy Typography Style -->
        <Style x:Key="GateHierarchyNameStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
            <!-- GATES: Major Headers -->
            <Style.Triggers>
                <DataTrigger Binding="{Binding RowType}" Value="Gate">
                    <Setter Property="FontSize" Value="20"/>
                    <!-- Increased from 18 -->
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
                <!-- TASKS: Sub-Headers -->
                <DataTrigger Binding="{Binding RowType}" Value="Task">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Foreground" Value="#EAEAEA"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2F35"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="#2A2F35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ADD THESE MISSING STYLES -->
        <Style x:Key="InlineTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="Transparent"/>
            <!-- Change: Transparent by default -->
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#D1D5DB"/>
            <!-- Slightly muted -->
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="4">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                            </Border>
                            
                            <!-- Placeholder Text -->
                            <TextBlock x:Name="Placeholder" 
                                       Text="{TemplateBinding Tag}" 
                                       Foreground="#6B7280" 
                                       FontStyle="Italic"
                                       IsHitTestVisible="False"
                                       Margin="9,4,8,4"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2A2F35"/>
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="#1A1D21"/>
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3C4148"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- 1. UNIFIED ROW TEMPLATE -->
        <DataTemplate DataType="{x:Type local:GateRowViewModel}">
            <StackPanel>
                <Border BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1" MinHeight="45"
                        AllowDrop="True" DragOver="Row_DragOver" DragLeave="Row_DragLeave" Drop="Row_Drop">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="Background" Value="#22FFD700"/>
                                    <!-- Selection Gold -->
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <!-- SELECTION TRIGGERS -->
                    <i:Interaction.Triggers>
                        <!-- BUGFIX: Changed from PreviewMouseLeftButtonDown to MouseLeftButtonDown.
                             The grip handle sets e.Handled=true on the TUNNELING (Preview) phase.
                             WPF propagates that Handled flag to the paired BUBBLING event, so
                             Interaction.Triggers (which respects Handled) skips this command
                             when the user clicks the grip. This preserves multi-row selection. -->
                        <i:EventTrigger EventName="MouseLeftButtonDown">
                            <i:InvokeCommandAction Command="{Binding DataContext.StartDragSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="MouseEnter">
                            <i:InvokeCommandAction Command="{Binding DataContext.HoverSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"/>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                            <i:InvokeCommandAction Command="{Binding DataContext.EndDragSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>

                    <Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/> <!-- Grip Column -->
                                <ColumnDefinition Width="3*" SharedSizeGroup="ColName"/>
                                <ColumnDefinition Width="2*" MinWidth="200" SharedSizeGroup="ColItem"/>
                                <ColumnDefinition Width="150" SharedSizeGroup="ColStart"/>
                                <ColumnDefinition Width="150" SharedSizeGroup="ColEnd"/>
                                <ColumnDefinition Width="120" SharedSizeGroup="ColStatus"/>
                                <ColumnDefinition Width="100" SharedSizeGroup="ColProgress"/>
                            </Grid.ColumnDefinitions>

                            <!-- 0. GRIP HANDLE -->
                            <Border Grid.Column="0" Width="12" Height="20" Background="{StaticResource GripBrush}" 
                                    Margin="10,0,5,0" Cursor="SizeAll" VerticalAlignment="Center"
                                    Tag="{Binding}" 
                                    PreviewMouseLeftButtonDown="Grip_PreviewMouseLeftButtonDown"
                                    PreviewMouseMove="Grip_PreviewMouseMove"/>

                            <!-- 1. EXPANDER & NAME (For Gate/Task/Block) -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" 
                    Margin="{Binding Level, Converter={StaticResource LevelToIndentConverter}}"
                    Visibility="{Binding RowType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=ChecklistItem:Inverse}">

                                <ToggleButton IsChecked="{Binding IsExpanded}" Style="{StaticResource ExpanderStyle}"
                          Visibility="{Binding Children.Count, Converter={StaticResource IntToVisibilityConverter}}"/>
                                <Border Width="24" Visibility="{Binding Children.Count, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=Inverse}"/>

                                <!-- Name Display (Gates/Tasks) -->
                                <TextBlock Text="{Binding Name}" 
                                           Style="{StaticResource GateHierarchyNameStyle}"
                                           Visibility="{Binding RowType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Block:Inverse}"/>

                                <!-- Block Edit Mode -->
                                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource InlineTextBoxStyle}" 
                                         FontWeight="SemiBold" FontSize="14"
                                         TextWrapping="Wrap" MaxWidth="300"
                                         Tag="&lt;Enter Task Name&gt;"
                                         Visibility="{Binding RowType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Block}"
                                         KeyDown="TextBox_KeyDown"/>
                                <!-- Add Item Button (Block level) -->
                                <Button Command="{Binding DataContext.AddItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                    CommandParameter="{Binding}" Margin="10,0,0,0" Style="{StaticResource ActionButtonStyle}"
                    Visibility="{Binding RowType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Block}">
                                    <TextBlock Text="+ sub-task" Foreground="#3B82F6" FontSize="11"/>
                                </Button>

                                <!-- Add Block Button (Gate/Task level) -->
                                <Button Command="{Binding DataContext.AddBlockCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                    CommandParameter="{Binding}" Margin="15,0,0,0" >
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RowType}" Value="Gate">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RowType}" Value="Task">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <TextBlock Text="+ task" Foreground="#10B981" FontSize="11" FontWeight="Bold"/>
                                </Button>
                            </StackPanel>

                            <!-- COLUMN 2: Only visible for ChecklistItems -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center"
                    Visibility="{Binding RowType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=ChecklistItem}">
                                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                      Style="{StaticResource InlineTextBoxStyle}" HorizontalAlignment="Stretch" Foreground="#EAEAEA"
                                         FontSize="13" FontWeight="Normal"
                       Tag="&lt;New Item&gt;"
                      KeyDown="TextBox_KeyDown"/>
                            </StackPanel>

                            <!-- SHARED COLUMNS (Visible for most types) -->
                            <TextBlock Grid.Column="3" Text="{Binding StartDate, StringFormat=MM/dd/yyyy}" Foreground="#9CA3AF" VerticalAlignment="Center" Visibility="{Binding IsDateVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <TextBlock Grid.Column="4" Text="{Binding EndDate, StringFormat=MM/dd/yyyy}" Foreground="#9CA3AF" VerticalAlignment="Center" Visibility="{Binding IsDateVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <!-- Progress Text (For Gates/Blocks/Tasks) -->
                            <TextBlock Grid.Column="5" Text="{Binding Progress, StringFormat=P0}" Foreground="#EAEAEA" FontWeight="Bold" VerticalAlignment="Center"
                                       Visibility="{Binding ShowProgressText, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <!-- Checkbox Logic (Only for Items or Empty Blocks) - Column 6 -->
                            <Grid Grid.Column="6" Visibility="{Binding ShowCheckbox, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <CheckBox IsChecked="{Binding IsCompleted}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>

                        <!-- INSERTION ADORNER (Top/Bottom highlights) -->
                        <Border x:Name="TopLine" BorderBrush="#3B82F6" BorderThickness="0,2,0,0" VerticalAlignment="Top" Visibility="Collapsed"/>
                        <Border x:Name="BottomLine" BorderBrush="#3B82F6" BorderThickness="0,0,0,2" VerticalAlignment="Bottom" Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <!-- RECURSIVE CHILDREN -->
                <ItemsControl ItemsSource="{Binding Children}" 
                               Visibility="{Binding IsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="20" Grid.IsSharedSizeScope="True">
        <!-- GLOBAL KEY BINDINGS -->
        <Grid.InputBindings>
            <KeyBinding Key="C" Modifiers="Ctrl" Command="{Binding CopySelectedCommand}"/>
            <KeyBinding Key="V" Modifiers="Ctrl" Command="{Binding PasteSelectedCommand}"/>
            <KeyBinding Key="Delete" Command="{Binding DeleteSelectedCommand}"/>
        </Grid.InputBindings>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Data Grid -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Command="{Binding BackCommand}" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Background="Transparent" BorderThickness="0" Margin="0,0,10,0">
                    <TextBlock Text="&#x2190;" FontSize="20" Foreground="#9CA3AF" FontWeight="Bold"/>
                </Button>
                <TextBlock Text="{Binding SubProjectName}" FontSize="24" FontWeight="Bold" Foreground="#EAEAEA" VerticalAlignment="Center"/>
                <Border Background="#3C4148" CornerRadius="4" Padding="8,4" Margin="15,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding SubProjectWbs}" Foreground="#9CA3AF" FontSize="12" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>

            <TextBlock Text="{Binding BreadcrumbText}" Foreground="#6B7280" FontSize="14" Margin="40,0,0,0"/>
        </StackPanel>

        <!-- 2. DATA TABLE HEADER -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column Headers -->
                <Grid Grid.Row="0" Background="{StaticResource SurfaceBrush}" Height="50">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/> <!-- Grip Column Placeholder -->
                        <ColumnDefinition Width="3*" SharedSizeGroup="ColName"/>
                        <ColumnDefinition Width="2*" MinWidth="200" SharedSizeGroup="ColItem"/>
                        <ColumnDefinition Width="150" SharedSizeGroup="ColStart"/>
                        <ColumnDefinition Width="150" SharedSizeGroup="ColEnd"/>
                        <ColumnDefinition Width="120" SharedSizeGroup="ColStatus"/>
                        <ColumnDefinition Width="100" SharedSizeGroup="ColProgress"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="1" Text="Gate / Task" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <TextBlock Grid.Column="2" Text="Sub-Task Item" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    <TextBlock Grid.Column="3" Text="Start Date" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="4" Text="End Date" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="5" Text="Status" Foreground="#9CA3AF" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="6" Text="Completed" Foreground="#9CA3AF" VerticalAlignment="Center"/>

                    <Border Grid.ColumnSpan="7" BorderBrush="#3C4148" BorderThickness="0,0,0,1" VerticalAlignment="Bottom"/>
                </Grid>

                <!-- Scrollable Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding RootRows}" Margin="0,0,0,20"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
