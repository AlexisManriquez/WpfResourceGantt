<UserControl x:Class="WpfResourceGantt.ProjectManagement.Features.Gantt.GanttView"
             x:Name="GanttViewRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfResourceGantt.ProjectManagement"
             xmlns:gantt="clr-namespace:WpfResourceGantt.ProjectManagement.Features.Gantt"
             xmlns:cv="clr-namespace:WpfResourceGantt.ProjectManagement.Converters"
             xmlns:sl="clr-namespace:WpfResourceGantt.ProjectManagement.Selectors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800"
             Background="{StaticResource AppBackgroundBrush}">
    <UserControl.Resources>
        <local:StringToIsNotEmptyConverter x:Key="StringToIsNotEmptyConverter"/>
        <cv:AutoFitWidthConverter x:Key="AutoFitWidthConverter"/>
        <cv:AutoFitMarginConverter x:Key="AutoFitMarginConverter"/>
        <cv:LeftMarginConverter x:Key="LeftMarginConverter"/>
        <cv:CanvasLeftConverter x:Key="CanvasLeftConverter"/>
        <cv:HealthToBrushConverter x:Key="HealthToBrushConverter"/>
        <cv:IndentationConverter x:Key="IndentationConverter" Indentation="15"/>
        <cv:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <cv:NegativeValueConverter x:Key="NegativeValueConverter"/>
        <cv:DoubleToGridLengthConverter x:Key="DoubleToGridLengthConverter"/>
        <cv:StringSplitConverter x:Key="StringSplitConverter"/>
        <cv:BindingProxy x:Key="ViewProxy" Data="{Binding ElementName=GanttViewRoot}"/>
        <!-- COLOR PALETTE (TACTICAL THEME) -->
        <SolidColorBrush x:Key="AppBackground" Color="#1E2329"/>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#C5A065"/> <!-- Gold accent -->
        <SolidColorBrush x:Key="TextDark" Color="#EAEAEA"/>
        <SolidColorBrush x:Key="TextGray" Color="#8C9095"/>
        <SolidColorBrush x:Key="BorderGray" Color="#3C4148"/>
        <SolidColorBrush x:Key="RowSeparator" Color="#23282E"/>
        <SolidColorBrush x:Key="HorizontalGridLineBrush" Color="#2D3238"/>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BorderBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- STYLE: Standard Header Button (Tactical) -->
        <Style x:Key="HeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3C4148"/>
            <Setter Property="Foreground" Value="#EAEAEA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4C5158"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- STYLE: Expander Toggle Arrow (Tactical) -->
        <Style x:Key="DetailsExpanderStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent" Width="20" Height="20">
                            <Path x:Name="Arrow" Data="M 0 0 L 4 4 L 0 8 Z" Stroke="#8C9095" StrokeThickness="1.5" 
                                  RenderTransformOrigin="0.5,0.5" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Arrow" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Arrow" Property="Stroke" Value="#C5A065"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Arrow" Property="Stroke" Value="#EAEAEA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Drag Adorner & Drop Styles -->
        <DataTemplate x:Key="DragAdornerTemplate" DataType="{x:Type local:WorkItem}">
            <Border Background="#23282E" BorderBrush="#C5A065" BorderThickness="1" CornerRadius="4" Padding="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding WbsValue}" FontWeight="SemiBold" Foreground="#EAEAEA" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding Name, Converter={StaticResource StringSplitConverter}}" Foreground="#EAEAEA"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <Style x:Key="DropTargetStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="Tag" Value="DropBefore">
                    <Setter Property="BorderThickness" Value="0,2,0,0"/>
                    <Setter Property="BorderBrush" Value="#C5A065"/>
                </Trigger>
                <Trigger Property="Tag" Value="DropAfter">
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    <Setter Property="BorderBrush" Value="#C5A065"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- NEW STYLE: Frozen Column Element -->
        <Style x:Key="FrozenColumnStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E2329"/>
            <Setter Property="Panel.ZIndex" Value="100"/>
            <Setter Property="BorderBrush" Value="{StaticResource HorizontalGridLineBrush}"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform X="{Binding Data.HorizontalOffset, Source={StaticResource ViewProxy}}"/>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- 1. Template for the items inside the dropdown list -->
        <DataTemplate x:Key="FilterDropdownItemTemplate">
            <TextBlock Text="{Binding Name}" Padding="5" Foreground="#EAEAEA"/>
        </DataTemplate>

        <!-- 2. Shared Style for Filter ComboBoxes -->
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Background" Value="White"/>
            <!-- Important: Prevents auto-selecting the first item -->
            <Setter Property="IsSynchronizedWithCurrentItem" Value="False"/>
            <Setter Property="ItemTemplate" Value="{StaticResource FilterDropdownItemTemplate}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Toggle Button (The Box) -->
                            <ToggleButton x:Name="ToggleButton" 
                                      Focusable="false"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#15191D" 
                                            BorderBrush="{StaticResource BorderGray}" 
                                            BorderThickness="1" 
                                            CornerRadius="4">
                                            <Path Data="M 0 0 L 4 4 L 8 0" Stroke="#8C9095" StrokeThickness="1.5" 
                                              HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- FIX: Direct Binding to SelectedItem.Name -->
<!-- This ensures we show exactly what is selected, not the first item -->
                            <TextBlock x:Name="DisplayedText"
                                   Text="{Binding SelectedItem.Name, RelativeSource={RelativeSource TemplatedParent}}"
                                   Margin="8,0,30,0"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   TextTrimming="CharacterEllipsis"/>

                            <!-- Placeholder Text (Bound to Tag property) -->
                            <TextBlock x:Name="PlaceholderText" 
                                   Text="{TemplateBinding Tag}" 
                                   Foreground="#9CA3AF" 
                                   Margin="8,0,0,0" 
                                   VerticalAlignment="Center" 
                                   IsHitTestVisible="False"
                                   Visibility="Collapsed"/>

                            <!-- Dropdown Popup -->
                            <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                                <Grid Name="DropDown" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="SelectionIndicator" 
                                            Background="{StaticResource PrimaryBlue}" 
                                            Width="4" HorizontalAlignment="Left" Visibility="Collapsed"/>
                                    <Border x:Name="DropDownBorder"
                                        Background="#23282E"
                                        BorderBrush="{StaticResource BorderGray}"
                                        BorderThickness="1"/>
                                    <ScrollViewer Margin="1" SnapsToDevicePixels="True" CanContentScroll="False">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>

                        <!-- Triggers -->
                        <ControlTemplate.Triggers>
                            <!-- Show Placeholder if SelectedItem is null -->
                            <DataTrigger Binding="{Binding SelectedItem, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="DisplayedText" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 1. CELL TEMPLATES: These define how the data looks for each column type -->
<!-- Health Cell -->
        <DataTemplate x:Key="HealthCellTemplate">
            <!-- The Border handles the vertical separator line -->
            <Border Width="{Binding Width}" BorderBrush="{StaticResource RowSeparator}" BorderThickness="0,0,1,0">
                <!-- We find the WorkItem via the Tag of the parent ContentPresenter -->
                <Ellipse Width="12" Height="12" 
                         Fill="{Binding Tag.WorkHealth, RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource HealthToBrushConverter}}" 
                         VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Border>
        </DataTemplate>

        <!-- SV Cell -->
        <DataTemplate x:Key="SVCellTemplate">
            <Border Width="{Binding Width}" BorderBrush="{StaticResource RowSeparator}" BorderThickness="0,0,1,0">
                <TextBlock Text="{Binding Tag.ScheduleVariancePercent, RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat=P0}" 
                           Foreground="{StaticResource TextGray}" FontSize="12" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>

        <!-- CV Cell -->
        <DataTemplate x:Key="CVCellTemplate">
            <Border Width="{Binding Width}" BorderBrush="{StaticResource RowSeparator}" BorderThickness="0,0,1,0">
                <TextBlock Text="{Binding Tag.CostVariancePercent, RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat=P0}" 
                           Foreground="{StaticResource TextGray}" FontSize="12" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>

        <!-- Predecessors Cell -->
        <DataTemplate x:Key="PredecessorsCellTemplate">
            <Border Width="{Binding Width}" BorderBrush="{StaticResource RowSeparator}" BorderThickness="0,0,1,0">
                <TextBlock Text="{Binding Tag.Predecessors, RelativeSource={RelativeSource AncestorType=ContentPresenter}}" 
                           Foreground="{StaticResource TextGray}" FontSize="11" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"/>
            </Border>
        </DataTemplate>

        <!-- Float Cell -->
        <DataTemplate x:Key="FloatCellTemplate">
            <Border Width="{Binding Width}" BorderBrush="{StaticResource RowSeparator}" BorderThickness="0,0,1,0">
                <TextBlock Text="{Binding Tag.TotalFloat, RelativeSource={RelativeSource AncestorType=ContentPresenter}}" 
                           Foreground="{StaticResource TextGray}" FontSize="12" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </DataTemplate>


        <!-- NEW: Placeholder Row Cell (Empty Gray Vertical Line) -->
        <DataTemplate x:Key="PlaceholderCellTemplate">
            <!-- Matches the visual of "Add New Column" row background -->
            <Border Width="{Binding Width}" BorderBrush="{StaticResource RowSeparator}" BorderThickness="0,0,1,0" Background="#1E2329"/>
        </DataTemplate>

        <!-- Update Selector with the new template -->
        <sl:ColumnCellTemplateSelector x:Key="ColumnCellTemplateSelector"
                                          HealthTemplate="{StaticResource HealthCellTemplate}"
                                          SVTemplate="{StaticResource SVCellTemplate}"
                                          CVTemplate="{StaticResource CVCellTemplate}"
                                          
                                           PredecessorsTemplate="{StaticResource PredecessorsCellTemplate}"
                                           FloatTemplate="{StaticResource FloatCellTemplate}"
                                           PlaceholderTemplate="{StaticResource PlaceholderCellTemplate}"/>

        <!-- 2. HEADER MENU STYLE -->
<!-- This defines how the "Add Column" or "Settings" menu looks -->
        <Style x:Key="ColumnHeaderButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <!-- Down Arrow Icon -->
                            <Path Data="M 0 0 L 4 4 L 8 0" Stroke="#64748B" StrokeThickness="1.5" 
                                  VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Row for the user selector -->
            <RowDefinition Height="*" />
            <!-- Row for the main Gantt chart -->
        </Grid.RowDefinitions>

        <!-- 1. TOP HEADER (Tactical) -->
        <Border Grid.Row="0" Background="{StaticResource AppBackground}" BorderBrush="{StaticResource BorderGray}" BorderThickness="0,0,0,1" Padding="10,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Title/Filter -->
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <!-- Actions -->
                </Grid.ColumnDefinitions>

                <!-- Left: Filter & Search -->
                <TextBlock Grid.Column="0" Text="Project Gantt" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,0,20,0" VerticalAlignment="Center"/>

                <!-- Middle: Filters -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- System Filter -->
                    <ComboBox ItemsSource="{Binding SystemOptions}"
              SelectedItem="{Binding SelectedSystemFilter}"
              Style="{StaticResource FilterComboBoxStyle}"
              Tag="System..."/>

                    <!-- Project Filter -->
                    <ComboBox ItemsSource="{Binding ProjectOptions}"
              SelectedItem="{Binding SelectedProjectFilter}"
              IsEnabled="{Binding SelectedSystemFilter.Id, Converter={StaticResource StringToIsNotEmptyConverter}}"
              Style="{StaticResource FilterComboBoxStyle}"
              Tag="Project..."/>

                    <!-- SubProject Filter -->
                    <ComboBox ItemsSource="{Binding SubProjectOptions}"
              SelectedItem="{Binding SelectedSubProjectFilter}"
              IsEnabled="{Binding SelectedProjectFilter.Id, Converter={StaticResource StringToIsNotEmptyConverter}}"
              Style="{StaticResource FilterComboBoxStyle}"
              Tag="Subproject..."/>
                    <Button Command="{Binding ClearFiltersCommand}" 
ToolTip="Reset Filters"
Style="{StaticResource ActionButtonStyle}"
Margin="5,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✕" Foreground="{StaticResource StatusBusyBrush}" FontSize="14" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Clear" Foreground="{StaticResource TacticalTextMutedBrush}" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <!-- Search Box -->
                    <TextBlock Text="Search:" VerticalAlignment="Center" Foreground="{StaticResource TextGray}" Margin="15,0,5,0" FontSize="12"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
             Width="120" Height="28" VerticalContentAlignment="Center" Padding="5,0"
             BorderBrush="{StaticResource BorderGray}">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                </StackPanel>

                <!-- Right: Actions & Zoom -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Zoom Slider -->
                    <TextBlock Text="Zoom" VerticalAlignment="Center" Foreground="{StaticResource TextGray}" FontSize="11" Margin="0,0,5,0"/>
                    <Slider x:Name="ZoomSlider"
                            Value="{Binding ZoomLevel, Mode=TwoWay}"
                            Minimum="10" Maximum="400" Width="100"
                            VerticalAlignment="Center" Margin="0,0,20,0"/>

                    <!-- User Selector -->
                    <ComboBox ItemsSource="{Binding GroupedUsers}"
                              SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                              DisplayMemberPath="Name"
                              Width="160" Height="30" 
                              VerticalContentAlignment="Center" Padding="6,0"
                              Margin="0,0,15,0">
                        <ComboBox.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" 
                               Foreground="{StaticResource TextGray}" Margin="5,2"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ComboBox.GroupStyle>
                    </ComboBox>

                    <!-- Action Buttons -->
                    <Button Content="Import Hours" Command="{Binding ImportHoursCommand}" Style="{StaticResource HeaderButtonStyle}"/>
                    <Button Content="Expand All" Command="{Binding ExpandAllCommand}" Style="{StaticResource HeaderButtonStyle}"/>
                    <Button Content="Collapse All" Command="{Binding CollapseAllCommand}" Style="{StaticResource HeaderButtonStyle}"/>
                    <Button Content="Fit to Screen" 
                            Command="{Binding FitToScreenCommand}"
                            CommandParameter="{Binding ActualWidth, ElementName=GanttTree}"
                            Style="{StaticResource HeaderButtonStyle}"
                            Margin="0"/>
                    <!-- Last button no margin -->
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 2. GANTT CONTENT -->
        <Grid Grid.Row="1">

            <Grid.RowDefinitions>
                <!-- Row 0: "Task" and "Timeline" headers -->
                <RowDefinition Height="Auto"/>
                <!-- Row 1: The scrollable area -->
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <!-- =========================== -->
<!-- ROW 0: HEADERS              -->
<!-- =========================== -->
            <Grid Grid.Row="0" Background="{StaticResource SurfaceBrush}" Panel.ZIndex="100">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <!-- 0. Task Name -->
                    <ColumnDefinition Width="Auto" />
                    <!-- 1. DYNAMIC COLUMNS -->
                    <ColumnDefinition Width="*" />
                    <!-- 2. Timeline -->
                </Grid.ColumnDefinitions>

                <!-- STATIC HEADER: Task Name -->
                <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,1" Height="25" Padding="10,0">
                    <Grid>
                        <TextBlock Text="Task Name" FontWeight="Bold" Foreground="{StaticResource TextDark}" FontSize="12" VerticalAlignment="Center"/>

                        
                    </Grid>
                </Border>

                <!-- DYNAMIC HEADERS (Column 1) -->
                <ItemsControl Grid.Column="1" ItemsSource="{Binding VisibleColumns}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- 1. NAME THE BORDER for Triggers -->
                                    <Border x:Name="HeaderBorder"
                                    Width="{Binding Width}" 
                                    Height="25"
                                    BorderBrush="{StaticResource BorderBrush}" 
                                    BorderThickness="0,0,1,1" 
                                    Background="{StaticResource SurfaceBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- HEADER TEXT -->
                                    <TextBlock Grid.Column="0" 
                                               Text="{Binding Header}" 
                                               FontWeight="Bold" 
                                               Foreground="{StaticResource TextDark}" 
                                               FontSize="12" 
                                               VerticalAlignment="Center" 
                                               Margin="10,0,0,0"
                                               TextTrimming="CharacterEllipsis"/>

                                    <!-- DROPDOWN ARROW BUTTON -->
<!-- 2. Set Default Visibility to HIDDEN -->
<!-- 3. Add Click Event -->
                                    <Button x:Name="DropdownButton"
                                            Grid.Column="1" 
                                            Style="{StaticResource ColumnHeaderButtonStyle}" 
                                            Margin="0,0,2,0"
                                            Visibility="Hidden" 
                                            Click="HeaderDropdown_Click">

                                        <Button.ContextMenu>
                                            <ContextMenu>
                                                <ContextMenu.Resources>
                                                    <Style TargetType="MenuItem">
                                                        <Setter Property="FontSize" Value="12"/>
                                                        <Setter Property="Padding" Value="10,5"/>
                                                    </Style>
                                                </ContextMenu.Resources>

                                                <!-- === PLACEHOLDER MODE: Show List of Columns to Add === -->
<!-- Header Label -->
                                                <MenuItem Header="Insert Column" 
                                                          FontWeight="Bold" 
                                                          IsEnabled="False"
                                                          Visibility="{Binding IsPlaceholder, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                                <!-- Dynamic List of Available Columns -->
                                                <MenuItem Header="Select Field..." 
                                                          ItemsSource="{Binding Data.DataContext.AvailableColumns, Source={StaticResource ViewProxy}}"
                                                          Visibility="{Binding IsPlaceholder, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <MenuItem.ItemContainerStyle>
                                                        <Style TargetType="MenuItem">
                                                            <Setter Property="Header" Value="{Binding Header}"/>
                                                            <Setter Property="Command" Value="{Binding Data.DataContext.AddColumnCommand, Source={StaticResource ViewProxy}}"/>
                                                            <Setter Property="CommandParameter" Value="{Binding}"/>
                                                        </Style>
                                                    </MenuItem.ItemContainerStyle>
                                                </MenuItem>

                                                <!-- === EXISTING COLUMN MODE: Show Hide Option === -->
                                                <MenuItem Header="Column Settings" 
                                                          FontWeight="Bold" 
                                                          IsEnabled="False"
                                                          Visibility="{Binding IsPlaceholder, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}"/>

                                                <MenuItem Header="Hide Column"
                                                          Command="{Binding Data.DataContext.RemoveColumnCommand, Source={StaticResource ViewProxy}}"
                                                          CommandParameter="{Binding}"
                                                          Visibility="{Binding IsPlaceholder, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                                                    <MenuItem.Icon>
                                                        <TextBlock Text="?" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>

                                            </ContextMenu>
                                        </Button.ContextMenu>
                                    </Button>
                                </Grid>
                            </Border>

                            <!-- 4. TRIGGERS TO HANDLE HOVER VISIBILITY -->
                            <DataTemplate.Triggers>
                            <!-- Trigger 1: Mouse Over the Header Border -> Show Button -->
                            <Trigger SourceName="HeaderBorder" Property="IsMouseOver" Value="True">
                                <Setter TargetName="DropdownButton" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <!-- Trigger 2: Menu is Open -> Keep Button Visible -->
<!-- FIX: Use DataTrigger to bind to the ContextMenu.IsOpen property -->
                            <DataTrigger Binding="{Binding ContextMenu.IsOpen, ElementName=DropdownButton}" Value="True">
                                <Setter TargetName="DropdownButton" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="DropdownButton" Property="Background" Value="#E2E8F0"/>
                            </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- TIMELINE HEADERS -->
                <ScrollViewer Grid.Column="2" 
                      x:Name="TimelineHeaderScrollViewer"
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollBarVisibility="Disabled"
                      HorizontalContentAlignment="Left">

                    <Border x:Name="TimelineHeaderContainer" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="0,0,0,1"
                            Background="{StaticResource SurfaceBrush}"
                            Width="{Binding TimelineWidth}"
                            HorizontalAlignment="Left">
                        <!-- IMPORTANT: Bind Width to VM calculation -->

                        <ItemsControl ItemsSource="{Binding TimelineSegments}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <!-- Canvas allows absolute positioning via Canvas.Left -->
                                    <Canvas ClipToBounds="True" Height="25" 
                                            Width="{Binding Data.DataContext.TimelineWidth, Source={StaticResource ViewProxy}}"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <!-- Use ItemContainerStyle to set Canvas.Left on the ContentPresenter wrapper -->
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource CanvasLeftConverter}">
                                                <Binding Path="StartDate"/>
                                                <Binding Path="Data.DataContext.ProjectStartDate" Source="{StaticResource ViewProxy}"/>
                                                <Binding Path="Data.DataContext.ProjectEndDate" Source="{StaticResource ViewProxy}"/>
                                                <Binding Path="Data.DataContext.TimelineWidth" Source="{StaticResource ViewProxy}"/>
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ItemsControl.ItemContainerStyle>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Height="25" 
                                            BorderBrush="{StaticResource BorderBrush}" 
                                            BorderThickness="1,0,0,0">
                                        <!-- Vertical Separator -->
<!-- WIDTH CALCULATION -->
                                        <Border.Width>
                                            <MultiBinding Converter="{StaticResource AutoFitWidthConverter}" 
                                                          ConverterParameter="Inclusive">
                                                <Binding Path="StartDate"/>
                                                <Binding Path="EndDate"/>
                                                <Binding Path="Data.DataContext.ProjectStartDate" Source="{StaticResource ViewProxy}"/>
                                                <Binding Path="Data.DataContext.ProjectEndDate" Source="{StaticResource ViewProxy}"/>
                                                <Binding Path="Data.DataContext.TimelineWidth" Source="{StaticResource ViewProxy}"/>
                                            </MultiBinding>
                                        </Border.Width>

                                        <!-- TEXT -->
                                        <TextBlock Text="{Binding Name}" 
                                                   Foreground="{StaticResource TacticalTextMutedBrush}" 
                                                   FontSize="11" 
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Bottom" 
                                                   Margin="0,0,0,2"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </ScrollViewer>
            </Grid>

            <!-- No Assignments Placeholder -->
            <StackPanel Grid.Row="1" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Orientation="Vertical"
                        Panel.ZIndex="10"
                        Visibility="{Binding HasAssignments, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=invert}">
                <TextBlock Text="No assignments available" 
                           FontSize="18" 
                           Foreground="{StaticResource TextGray}" 
                           HorizontalAlignment="Center"
                           FontWeight="SemiBold"/>
            </StackPanel>

            <!-- The Data -->
            <TreeView Grid.Row="1" 
              x:Name="GanttTree"
              Background="Transparent"
              ItemsSource="{Binding WorkItems}" 
              BorderThickness="0"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.ScrollChanged="GanttTree_ScrollChanged"
              VirtualizingStackPanel.IsVirtualizing="True"
              VirtualizingStackPanel.VirtualizationMode="Recycling"
              VirtualizingStackPanel.ScrollUnit="Pixel"
                      ScrollViewer.CanContentScroll="True"
          VirtualizingStackPanel.CacheLengthUnit="Page"
          VirtualizingStackPanel.CacheLength="2,2">

                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="0"/>

                        <!-- Events -->
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="TreeViewItem_PreviewMouseLeftButtonDown"/>
                        <EventSetter Event="PreviewMouseMove" Handler="TreeViewItem_PreviewMouseMove"/>
                        <EventSetter Event="Drop" Handler="TreeViewItem_Drop"/>
                        <EventSetter Event="DragOver" Handler="TreeViewItem_DragOver"/>
                        <Setter Property="AllowDrop" Value="True"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TreeViewItem">
                                    <StackPanel>
                                        <Grid x:Name="RowGrid" MinHeight="36">
                                            <!-- 1. Background / Row Border -->
                                            <Border BorderBrush="{StaticResource HorizontalGridLineBrush}" BorderThickness="0,0,0,1" 
                    Background="{TemplateBinding Background}" />

                                            <!-- 2. RESTORED: Drop Indicator -->
<!-- This is the blue line that appears when dragging -->
                                            <Border x:Name="DropIndicator" 
                                                    Height="2" 
                                                    Background="{StaticResource PrimaryBlue}" 
                                                    Visibility="Collapsed" 
                                                    Panel.ZIndex="999" 
                                                    IsHitTestVisible="False"/>

                                            <!-- 3. Main Row Content (The 5 columns) -->
                                            <ContentPresenter x:Name="HeaderContent" ContentSource="Header" VerticalAlignment="Center"/>

                                            <!-- 4. The Expander Arrow (Tree Arrow) -->
                                            <ToggleButton x:Name="Expander" 
                                                          Width="30"
                                                          Style="{StaticResource DetailsExpanderStyle}"
                                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" 
                                                          ClickMode="Press" 
                                                          HorizontalAlignment="Left" 
                                                          VerticalAlignment="Center"
                                                          Panel.ZIndex="250"
                                                          Background="Transparent"
                                                          Margin="0">
                                                <ToggleButton.RenderTransform>
                                                    <TransformGroup>
                                                        <!-- Scroll Logic -->
                                                        <TranslateTransform X="{Binding Data.HorizontalOffset, Source={StaticResource ViewProxy}}"/>

                                                        <!-- Hierarchy Logic -->
                                                        <TranslateTransform X="{Binding Level, Converter={StaticResource IndentationConverter}}"/>
                                                    </TransformGroup>
                                                </ToggleButton.RenderTransform>
                                            </ToggleButton>
                                        </Grid>

                                        <!-- THE CHILDREN CONTAINER (FLATTENED) -->
                                        <ItemsPresenter x:Name="ItemsHost" Visibility="Collapsed" Margin="0"/>
                                    </StackPanel>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="HasItems" Value="False">
                                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                        </Trigger>
                                        <Trigger Property="IsExpanded" Value="True">
                                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                                        </Trigger>

                                        <!-- RESTORED: Drop Visual Triggers -->
                                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="DropBefore">
                                            <Setter TargetName="DropIndicator" Property="Visibility" Value="Visible"/>
                                            <Setter TargetName="DropIndicator" Property="VerticalAlignment" Value="Top"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="DropAfter">
                                            <Setter TargetName="DropIndicator" Property="Visibility" Value="Visible"/>
                                            <Setter TargetName="DropIndicator" Property="VerticalAlignment" Value="Bottom"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsSystem}" Value="True">
                                            <Setter TargetName="RowGrid" Property="Background" Value="#1A1E23"/>
                                        </DataTrigger>

                                        <!-- Hover Effect -->
                                        <Trigger Property="IsMouseOver" Value="True" SourceName="RowGrid">
                                            <Setter Property="Background" Value="{StaticResource RowSeparator}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type local:WorkItem}" ItemsSource="{Binding Children}">
                        <Grid MinHeight="36">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <!-- Main Task Row -->
                                <RowDefinition Height="Auto"/>
                                <!-- Expanded Details Row -->
                            </Grid.RowDefinitions>
                            <!-- Set specific row height for consistency -->
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="300" />
                                <!-- Task -->
                                <ColumnDefinition Width="Auto" />
                                <!-- Dynamic -->
                                <ColumnDefinition Width="Auto" />
                                <!-- Timeline Bar -->
                            </Grid.ColumnDefinitions>

                            <!-- 1. Task Info -->
                            <Border Grid.Row="0" Grid.Column="0" 
        Style="{StaticResource FrozenColumnStyle}"
        BorderThickness="0,0,1,1"
        Height="36">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <!-- Only show Delete if it's a System -->
                                        <MenuItem Header="Delete System" 
                      Command="{Binding Data.DataContext.DeleteSystemCommand, Source={StaticResource ViewProxy}}"
                      CommandParameter="{Binding}"
                      Visibility="{Binding IsSystem, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Icon>
                                                <TextBlock Text="??" Foreground="Red" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </MenuItem.Icon>
                                        </MenuItem>

                                        <!-- You can add other row-level actions here later -->
                                        <MenuItem Header="Expand All" Command="{Binding Data.DataContext.ExpandAllCommand, Source={StaticResource ViewProxy}}"/>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <!-- 1. Indentation Spacer (Level * 20) -->
                                        <ColumnDefinition Width="{Binding Level, Converter={StaticResource IndentationConverter}}"/>
                                        <!-- 2. Tree Arrow Spacer (Reduced to 18 for tighter look) -->
                                        <ColumnDefinition Width="30"/>
                                        <!-- 4. Task Name -->
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 1. Details Toggle (Clipboard) -->
<!--
                                    <ToggleButton Grid.Column="1"
                      Style="{StaticResource DetailsExpanderStyle}"
                      IsChecked="{Binding AreProgressBlocksVisible}"
                      Visibility="{Binding IsLeaf, Converter={StaticResource BooleanToVisibilityConverter}}"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center"/>
                                    -->
                                    <DockPanel Grid.Column="2" VerticalAlignment="Center" Margin="5,0,10,0">
                                        <!-- Open Gate Progress (Sub-Projects only) -->
                                        <Button DockPanel.Dock="Right"
                                                Command="{Binding Data.DataContext.OpenGateProgressCommand, Source={StaticResource ViewProxy}}"
                                                CommandParameter="{Binding}"
                                                Visibility="{Binding IsSubProject, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                ToolTip="Open Gate Progress Dashboard"
                                                Background="Transparent" BorderThickness="0" Cursor="Hand"
                                                Width="24" Height="24" Margin="5,0,0,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Opacity" Value="0.7"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="1.0"/>
                                                            <Setter Property="Background" Value="#10000000"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Path Data="M14,3V5H17.59L7.76,14.83L9.17,16.24L19,6.41V10H21V3M19,19H5V5H12V3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V12H19V19Z" 
                                                  Fill="#C5A065" Stretch="Uniform" Width="14" Height="14"/>
                                        </Button>


                                        <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextDark}" TextTrimming="CharacterEllipsis"/>
                                    </DockPanel>
                                </Grid>
                            </Border>

                            <!-- 2. DYNAMIC COLUMNS (Replaces Health, SV, CV borders) -->
<!-- We wrap in Border with FrozenColumnStyle so it floats correctly when scrolling right -->
                            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource FrozenColumnStyle}" 
                                    BorderThickness="0,0,0,1">

                                <ItemsControl ItemsSource="{Binding Data.DataContext.VisibleColumns, Source={StaticResource ViewProxy}}"
                                              ItemTemplateSelector="{StaticResource ColumnCellTemplateSelector}"
                                              Visibility="{Binding IsSystem, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=invert}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>

                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <!-- CRITICAL: Pass the Row's WorkItem into the Cell Template via Tag -->
<!-- The DataContext of the ContentPresenter is the GanttColumn -->
<!-- We bind Tag to the TreeViewItem's DataContext (The WorkItem) -->
                                            <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=TreeViewItem}}"/>
                                            
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                </ItemsControl>
                            </Border>

                            <!-- ============================================= -->
<!--   Column 1: Timeline Gantt Bar              -->
<!-- ============================================= -->
                            <Border Grid.Row="0" Grid.Column="2" 
                    Panel.ZIndex="0" 
                    BorderBrush="{StaticResource HorizontalGridLineBrush}" 
                    BorderThickness="0,0,0,1"
                    Width="{Binding Data.DataContext.TimelineWidth, Source={StaticResource ViewProxy}}"
                    Visibility="{Binding IsSystem, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=invert}">
                                <gantt:AnimatedGanttBar VerticalAlignment="Center" Height="28"
                                        Progress="{Binding Progress}"
                                        StartDate="{Binding StartDate}"
                                        EndDate="{Binding EndDate}"
                                        IsSummary="{Binding IsSummary}"
                                        IsMilestone="{Binding IsMilestone}"
                                        IsCritical="{Binding IsCritical}"
                                        ScheduleHealth="{Binding ScheduleHealth}"
                                        IsBaselined="{Binding IsBaselined}"
                                        BaselineStartDate="{Binding BaselineStartDate}"
                                        BaselineEndDate="{Binding BaselineEndDate}"
                                        HealthStatus="{Binding WorkHealth}" 
                                        TotalTimelineStart="{Binding Data.DataContext.ProjectStartDate, Source={StaticResource ViewProxy}}"
                                        TotalTimelineEnd="{Binding Data.DataContext.ProjectEndDate, Source={StaticResource ViewProxy}}">
                                    <!-- NEW: RICH TOOLTIP -->
                                    <gantt:AnimatedGanttBar.ToolTip>
                                        <ToolTip Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="10">
                                            <StackPanel>
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="Bold"/>

                                               

                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <!-- Label -->
                                                        <ColumnDefinition Width="*"/>
                                                        <!-- Value -->
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <!-- Row 0: Dates -->
                                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Start:" Foreground="{StaticResource TacticalTextMutedBrush}" Margin="0,0,10,2"/>
                                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding StartDate, StringFormat='MMM dd, yyyy'}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>

                                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="End:" Foreground="{StaticResource TacticalTextMutedBrush}" Margin="0,0,10,5"/>
                                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding EndDate, StringFormat='MMM dd, yyyy'}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="SemiBold"/>

                                                    <!-- Row 2: Progress -->
                                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Progress:" Foreground="{StaticResource TacticalTextMutedBrush}" Margin="0,0,10,5"/>
                                                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding Progress, StringFormat=P0}" Foreground="{StaticResource TextPrimaryBrush}" FontWeight="Bold"/>
                                                        <TextBlock Text=" Complete" Foreground="{StaticResource TacticalTextMutedBrush}" FontSize="11" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                                    </StackPanel>

                                                    <!-- Row 3: Work (Hours) -->
                                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Work:" Foreground="{StaticResource TacticalTextMutedBrush}" Margin="0,0,10,2"/>
                                                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding WorkDisplay}" Foreground="{StaticResource TacticalTextMutedBrush}"/>
                                                        <TextBlock Text=" hrs" Foreground="{StaticResource TacticalTextMutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <!-- Divider -->
                                                    <Rectangle Grid.Row="4" Grid.ColumnSpan="2" Height="1" Fill="#F3F4F6" Margin="0,8"/>

                                                    <!-- Row 5: Personnel (Developer - Only visible if Leaf) -->
                                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Developer:" Foreground="#6B7280" Margin="0,0,10,2"
                               Visibility="{Binding IsLeaf, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding DeveloperName, TargetNullValue='Unassigned'}" 
                               Visibility="{Binding IsLeaf, Converter={StaticResource BooleanToVisibilityConverter}}" FontWeight="SemiBold"/>

                                                    <!-- Row 6 & 7: Personnel (Management - Only visible if Summary/System) -->
                                                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Manager:" Foreground="#6B7280" Margin="0,0,10,2"
                               Visibility="{Binding IsSummary, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding ProjectManagerName, TargetNullValue='--'}" 
                               Visibility="{Binding IsSummary, Converter={StaticResource BooleanToVisibilityConverter}}" FontWeight="SemiBold"/>

                                                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Chief:" Foreground="#6B7280" Margin="0,0,10,2"
                               Visibility="{Binding IsSummary, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                    <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding SectionChiefName, TargetNullValue='--'}" 
                               Visibility="{Binding IsSummary, Converter={StaticResource BooleanToVisibilityConverter}}" FontWeight="SemiBold"/>

                                                    <!-- Row 8: Variances -->
                                                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Variance:" Foreground="#6B7280" Margin="0,5,10,0"/>
                                                    <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Horizontal" Margin="0,5,0,0">
                                                        <TextBlock Text="SV: " Foreground="#6B7280" FontSize="11"/>
                                                        <TextBlock Text="{Binding ScheduleVariancePercent, StringFormat=P0}" FontWeight="Bold" Margin="0,0,10,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding ScheduleVariance}" Value="-0.01">
                                                                            <!-- Logic check needed here in future, relying on VM for now -->
<!-- Simple fallback color -->
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>

                                                        <TextBlock Text="CV: " Foreground="#6B7280" FontSize="11"/>
                                                        <TextBlock Text="{Binding CostVariancePercent, StringFormat=P0}" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </StackPanel>
                                        </ToolTip>
                                    </gantt:AnimatedGanttBar.ToolTip>
                                </gantt:AnimatedGanttBar>
                            </Border>
                            <!-- NEW: Details Row (Progress Blocks) -->
<!--<Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4"
                                            Visibility="{Binding AreProgressBlocksVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                                            Background="{StaticResource AppBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20" >
                                <Grid Margin="50,10,10,10">
                                    <ItemsControl ItemsSource="{Binding ProgressBlocks}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>

                                                        <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="15,10" Background="{StaticResource TacticalSeparatorBrush}" CornerRadius="8,8,0,0">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="??" Margin="0,0,10,0" Foreground="{StaticResource TacticalTextMutedBrush}"/>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                                <TextBlock Text="{Binding Items.Count, StringFormat=' ({0} items)'}" Foreground="{StaticResource TacticalTextMutedBrush}" Margin="5,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                                                            </StackPanel>
                                                        </Border>

                                                            <ItemsControl Grid.Row="1" ItemsSource="{Binding Items}" HorizontalContentAlignment="Stretch" VirtualizingPanel.IsVirtualizing="False">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Border BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1" Padding="15,10">
                                                                            <Grid>
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                </Grid.ColumnDefinitions>

                                                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                                                    <Ellipse Width="8" Height="8" Fill="#EF4444" Margin="0,0,10,0"/>
                                                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                                                </StackPanel>

                                                                                <Border Grid.Column="1" CornerRadius="12" Padding="10,4" MinWidth="90" HorizontalAlignment="Right">
                                                                                    <Border.Style>
                                                                                        <Style TargetType="Border">
                                                                                            <Setter Property="Background" Value="#DBEAFE"/>
                                                                                            <Style.Triggers>
                                                                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                                                                </DataTrigger>
                                                                                            </Style.Triggers>
                                                                                        </Style>
                                                                                    </Border.Style>
                                                                                    <TextBlock HorizontalAlignment="Center" FontWeight="SemiBold" FontSize="12">
                                                                                        <TextBlock.Style>
                                                                                            <Style TargetType="TextBlock">
                                                                                                <Setter Property="Text" Value="In Progress"/>
                                                                                                <Setter Property="Foreground" Value="#1E40AF"/>
                                                                                                <Style.Triggers>
                                                                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                                                        <Setter Property="Text" Value="Completed"/>
                                                                                                        <Setter Property="Foreground" Value="#065F46"/>
                                                                                                    </DataTrigger>
                                                                                                </Style.Triggers>
                                                                                            </Style>
                                                                                        </TextBlock.Style>
                                                                                    </TextBlock>
                                                                                </Border>
                                                                            </Grid>
                                                                        </Border>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="No progress checklist items defined." 
                               FontStyle="Italic" Foreground="#94A3B8" FontSize="11"
                               HorizontalAlignment="Left" Margin="10,0,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ProgressBlocks.Count}" Value="0">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>-->
                            
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            <!-- Inside the main Grid, Column 4, Row 1 -->
<!-- Replace your current Today Line block with this -->
            <Grid Grid.Row="1" IsHitTestVisible="False">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <!-- Task -->
                    <ColumnDefinition Width="Auto"/>
                    <!-- Dynamic -->
                    <ColumnDefinition Width="*"/>
                    <!-- Timeline -->
                </Grid.ColumnDefinitions>
                <ItemsControl Grid.Column="1" 
                              ItemsSource="{Binding VisibleColumns}"
                              Visibility="Hidden">
                    <!-- Hidden means it takes up space but isn't drawn -->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Match the width of the column -->
                            <Grid Width="{Binding Width}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <!-- The Today Line is only allowed to exist in the last column (Column 4) -->
                <!-- Dependency Arrows Overlay -->
                <gantt:DependencyArrowCanvas x:Name="DependencyArrows"
                    Grid.Column="2"
                    ClipToBounds="True"
                    IsHitTestVisible="False"
                                              VerticalAlignment="Stretch"
                    WorkItems="{Binding WorkItems}"
                    TimelineStart="{Binding ProjectStartDate}"
                    TimelineEnd="{Binding ProjectEndDate}"
                    TimelineWidth="{Binding TimelineWidth}"
                    HorizontalOffset="{Binding Data.HorizontalOffset, Source={StaticResource ViewProxy}}"
                    TreeViewReference="{Binding ElementName=GanttTree}"/>

                <Canvas Grid.Column="2" ClipToBounds="True">
                    <Border Width="2" 
                Background="#EF4444" 
                VerticalAlignment="Stretch"
                Height="{Binding ActualHeight, ElementName=GanttTree}"
                Panel.ZIndex="99"
                Visibility="{Binding IsTodayLineVisible, Converter={StaticResource BooleanToVisibilityConverter}}">

                        <!-- Positioning Logic -->
                        <Canvas.Left>
                            <Binding Path="TodayLinePosition"/>
                        </Canvas.Left>

                        <!-- Sync Logic: Moves line left as you scroll right -->
                        <Border.RenderTransform>
                            <TranslateTransform X="{Binding Data.HorizontalOffset, Source={StaticResource ViewProxy}, Converter={StaticResource NegativeValueConverter}}"/>
                        </Border.RenderTransform>

                        <!-- Today Label -->
                        <Canvas>
                            <Border Background="#EF4444" Padding="4,2" CornerRadius="0,4,4,4">
                                <TextBlock Text="Today" FontSize="9" FontWeight="Bold" Foreground="White"/>
                            </Border>
                        </Canvas>
                    </Border>
                </Canvas>
            </Grid>
        </Grid>
        
        </Grid>
</UserControl>
